---
version: '3'

services:
  wiremock:
    image: wiremock/wiremock
    command: "--verbose"
    ports:
      - "127.0.0.1:8080:8080"
      - "127.0.0.1:8443:8443"
    volumes:
      - ./wiremock:/home/wiremock
