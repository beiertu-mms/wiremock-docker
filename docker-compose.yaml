---
version: '3'

services:
  wiremock:
    image: wiremock/wiremock
    command: "--verbose"
    network_mode: host
    expose:
      - 8080:8080
      - 8443:8443
    volumes:
      - ./wiremock:/home/wiremock
